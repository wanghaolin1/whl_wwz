#include"new_tell.h"
int main()
{
	while(1)
	{
		show_menu();
		switch(get_cmd('1','6'))
		{
			case '1': add_cons(); 	 break;
			case '2': del_cons();	 break;
			case '3': change_cons(); break;
			case '4': find_cons(); 	 break;
			case '5': list_const();  break;
			case '6': return 0;
		}
	}
}
